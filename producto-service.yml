server:
   port: ${PORT:8000}

spring:
   cloud:
      stream:
         bindings:
            input: productoes

message: MENSAJE DESDE MEETUP LIVE CODING SESION

eureka:
   client:
      serviceUrl:
         defaultZone: http://localhost:1111/eureka

